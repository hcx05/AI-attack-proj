Ai-attack-proj/
├── README.md                  # 專案總覽、硬體分流（GPU 0/1）配置、基本評估指標
├── pyproject.toml             # 現代專案管理（ruff、black、pytest 規則鎖定）
├── requirements.txt           # 容器內部 pip 依賴清單
├── .gitignore                 # 嚴格排除 .env、內部日誌、假金鑰與向量庫
├── .env.example               # 本地 LLM API 端點與安全開關、Canary Token 樣板配置
├── docker-compose.yml         # 靶場多容器拓撲（Victim 預設斷網、Mock Web 獨立隔離）
├── Dockerfile.victim          # 藍軍靶機環境（封裝 NanoClaw、受限工具與 Policy Layer）
└── Dockerfile.mock_web        # 模擬網路環境（封網狀態下託管受控 HTML/Markdown）
│
├── docs/                      # ─── 【Phase 0：研究設計規格文件】 ───
│   ├── 00_scope_and_safety.md # 項目 0.1：安全契約與倫理邊界（In-Scope/Out-of-Scope 宣告）
│   ├── 01_topology_and_flow.md# 項目 0.2：系統拓撲、角色分工與信任邊界資料流向
│   ├── 02_exp_matrix.md       # 項目 0.3：實驗矩陣（注入強度、污染 Top-K、防禦開關之變數矩陣）
│   └── 03_metrics_and_log.md  # 項目 0.4：量化評估指標與日誌 Schema 標準定義
│
├── configs/                   # ─── 【靜態策略與 Schema 控制中心（不可寫入）】 ───
│   ├── victim_policy.json     # Victim 限制：可用工具、Shell 白名單、路徑黑名單、拒絕代碼
│   ├── planner_schema.json    # 限制 Planner (Qwen) 只能輸出嚴格符合實驗格式的 JSON 計畫
│   ├── payload_policy.json    # 限制 Payload Gen (Llama) 的文本生成範圍，防止真實惡意代碼溢出
│   ├── rag_config.json        # RAG 參數：Chunk Size, Overlap, Embedding 模型, 相似度閾值
│   └── logging_config.json    # 日誌分流、日誌輪轉（Log Rotation）與脫敏欄位定義
│
├── src/                       # ─── 【核心原始碼區】 ───
│   ├── orchestrator/          # ── 【確定的硬性控制層（絕對可信）】 ──
│   │   ├── __init__.py
│   │   ├── main.py            # 實驗總入口（讀取 Config、初始化環境、執行指定 Case）
│   │   ├── experiment_runner.py # 調度器（驅動 Planner -> Gen -> Victim -> Log 循環，每步必校驗）
│   │   └── circuit_breaker.py # 熔斷器（偵測模型死循環、異常網路、Token 暴漲時硬性終止實驗）
│   │
│   ├── safety/                # ── 【安全隔離與加固層（絕對可信，Phase 3-4 防禦主戰場）】 ──
│   │   ├── __init__.py
│   │   ├── policy_checker.py  # 策略檢查器（在 Tool 執行前，對名稱、參數進行動態防禦審查）
│   │   ├── path_guard.py      # 路徑守衛（正規化所有路徑，利用 commonpath 杜絕 Path Traversal）
│   │   ├── schema_validator.py# Schema 驗證器（強硬校驗紅隊 Agent 輸出的 JSON 結構）
│   │   ├── token_counter.py   # 【紅隊加固】Token 審計（防止 Context Overflow 與對齊溢出攻擊）
│   │   └── log_sanitizer.py   # 日誌淨化器（寫入公開 log 前，動態遮蔽/脫敏 Canary Token 與敏感詞）
│   │
│   ├── victim/                # ── 【藍軍受害者沙箱域（高度懷疑、受控執行）】 ──
│   │   ├── __init__.py
│   │   ├── agent.py           # Victim Agent 主體（介接 Llama-3.1-8B，執行 ReAct 攻防循環）
│   │   └── tools/             # ── 受限工具箱 ──
│   │       ├── __init__.py
│   │       ├── file_reader.py # 檔案讀取器（受 path_guard 限制，嚴禁讀取 secrets/ 或環境變數）
│   │       ├── web_fetch.py   # 網頁抓取器（強硬限制只允許存取 mock_server 的 Allowlist URL）
│   │       └── restricted_shell.py # 受限 Shell（僅允許 id、ls 等白名單參數，嚴禁溢出與無限制執行）
│   │
│   └── attacker/              # ── 【紅隊攻擊模擬域（生成不可信資料的源頭）】 ──
│       ├── __init__.py
│       ├── planner/           # ── 策略規劃（大腦：Qwen2.5-Instruct） ──
│       │   ├── __init__.py
│       │   └── agent.py       # Planner 介接（解構上一輪結果，輸出結構化測試 JSON 計畫）
│       └── payload_gen/       # ── 樣本生成（打手：Llama-3.1-8B-Instruct） ──
│           ├── __init__.py
│           └── agent.py       # Payload 生成介接（依 JSON 計畫生成對應的多格式、多語意測試文本）
│
├── data/                      # ─── 【實驗靜態與動態資料數據庫】 ───
│   ├── inputs_clean/          # 基準對照組：純淨網頁、標準企業流程與 FAQ 文件 Baseline
│   ├── inputs_generated/      # 實驗組暫存：Payload Gen 生成的注入 Payload 與污染文件
│   ├── secrets/               # 安全驗證邊界：放置 Mock Secrets 與特定的 Canary Token UUID
│   ├── rag_corpus/            # RAG 語料庫（後續細分為 clean/ 與 poisoned/ 子目錄）
│   └── vdb_storage/           # ChromaDB 持久化目錄（RAG 污染實驗 Phase 2 的主要目標區，Git 排除）
│
├── mock_server/               # ─── 【外部網路模擬環境（靶場外部邊界）】 ───
│   ├── server.py              # 輕量級本地 Web 伺服器（提供 Victim 唯一的外部攝入端點）
│   └── pages/                 # 模擬網頁目錄
│       ├── clean_page.html    # 正常對照組網頁
│       └── test_page.md       # 實驗組網頁（Orchestrator 依實驗矩陣動態寫入紅隊 Payload）
│
└── logs/                      # ─── 【可觀測性與統計數據產出區（應變數來源）】 ───
    ├── .gitkeep
    ├── run_history.csv        # 實驗總流水帳（Run ID, Case ID, 防禦開關, 成功/失敗, 耗時, 成本）
    ├── tool_calls.jsonl       # 工具調用事件軌跡（詳細記錄參數、Caller、是否被阻擋與 Block Reason）
    ├── policy_events.jsonl    # 安全策略攔截事件（路徑阻擋、Shell 越權、Canary 被觸發之細節）
    ├── rag_events.jsonl       # RAG 檢索事件細節（記錄特定 Query 下，污染 chunk 的 Rank 與 Score 漂移）
    ├── model_io.redacted.jsonl# 已脫敏的模型 I/O 審計紀錄（可用於提交 Git 或論文附錄展示）
    ├── circuit_breaker.log    # 熔斷日誌（記錄硬性終止條件觸發的時間戳與詳細原因）
    └── raw_untracked/         # 【紅隊加固】禁止 Git 追蹤區：存放原始 model_io.raw.jsonl（學術鑑識黃金標準）
│
└── tests/                     # ─── 【自動化安全與單元測試（Phase 0/1 核心：驗證底座穩定性）】 ───
    ├── test_policy_checker.py # 驗證 Policy Checker 是否能完美識別並攔截非白名單工具請求
    ├── test_path_guard.py     # 驗證 Path Guard 面對 `../../`、Symlink 與絕對路徑時的防禦成功率
    ├── test_file_reader.py    # 驗證 File Reader 在安全邊界內外讀取時的預期報錯與回傳行為
    ├── test_shell_executor.py # 驗證受限 Shell 對惡意多重參數、串接指令（如 `;`, `&&`）的靜態過濾防禦
    ├── test_planner_schema.py # 驗證當 Planner 輸出非預期垃圾格式時，Schema Validator 能正確拒絕
    ├── test_token_counter.py  # 【紅隊加固】驗證 Token 審計機制是否能精準攔截上下文溢出（Overflow）攻擊
    └── test_log_schema.py     # 驗證所有產出的 JSONL 符合標準 Schema，確保後續 Python 統計繪圖不會崩潰
